---
description: 打印单据中交接双方签字栏须留白，供当事人手签
globs: **/*Print*.cs,**/*Printing*.cs
alwaysApply: false
---

# 交接签字打印留白

凡打印单据中出现**交接双方签字**（当事人与资料室资料管理员/资料员），签字栏与日期栏**不得预填姓名或系统日期**，须留白供线下亲笔签名。

## 格式约定

硬盘介质等业务单常用两行格式：

```
{当事人标签}                                            日期:______年___月___日
资料室资料管理员签字：                                 日期:______年___月___日
```

当事人标签按单据语义取值，例如：`申请人签字`、`交接人签字`、`归还人签字`、`领用人签字` 等。

## 实现要求

- 打印数据装配（`*PrintData`、`*Printing*.cs`）与文档工厂（`*PrintDocumentFactory*.cs`）中，交接签字块固定输出空白栏，**禁止**将 `ApplicantName`、`ExecutedBy`、`BorrowerName` 等写入签字位。
- 审批意见、部门审核等**非交接双方**签字栏可另行规则；本规则仅约束实物/资料交接双方手签。
- 年度资料出库/归还交接单调用 `BuildHandoverPrintDataAsync`、`BuildReceiptPrintDataAsync` 时，`blankHandoverSignatures` 应恒为 `true`。

## 参考实现

- 硬盘介质：`ViewModels/HardDiskMedia/HardDiskMediaPrintDocumentFactory.cs` 中 `BuildBlankTwoPartyHandoverBlock`。
