---
description: 标准滑道式档案柜档口用途配置、启动行为与立档/归还落位校验
globs: Services/Cabinets/**,Services/YearlyArchive/**,App.xaml.cs,ViewModels/Cabinets/**,Services/SystemSettings/TestPreparationService.cs
alwaysApply: false
---

# 标准滑道式档案柜档口用途

## 语义

- 标准滑道式档案柜档口用于存放**档案盒内的模拟介质资料**（资料先入盒，再入档口）。
- 用途分类：`未设置`（默认，兼容「未定义」）、`年度资料专用档口`、`历史资料专用档口`。

## 用户配置

- 开柜界面通过 `ICabinetService.SetArchiveDedicatedSlotCategory` / `ClearArchiveDedicatedSlotCategory` 写入 `CabinetArchiveSlotCategoryAssignments`。
- **不得**在普通业务或应用启动时覆盖用户已保存的 `CategoryName`。
- 仅空档口（无档案盒占用）可变更用途。

## 启动时（`EnsureAllStandardArchiveSlotsUseUnsetCategoryOnStartup`）

- 仅为**尚未存在**用途记录的格口补默认「未设置」。
- **不**将已设为年度/历史资料专用的格口改回未设置。

## 立档 / 归还 / 迁档落位校验

共享规则：`ArchiveStorageSlotCategorySupport`（仅约束 `CabinetType.Standard`；立式/卧式不校验用途）。

| 业务 | 期望档口用途 | 钩子 |
|---|---|---|
| 年度模拟立档新建盒 | `年度资料专用档口` | `CreateArchiveBoxAsync` 硬校验；`SuggestArchiveBoxLocationAsync` 过滤候选 |
| 归还新建空盒 / 指定目标盒 / 办结归位 | `年度资料专用档口` | `CreateEmptyRehomeBoxAndAssignAsync`、`AssignRehomeTargetBoxAsync`、`GetRehomeTargetOptionsAsync`、办结改挂 |
| 年度模拟单件/批量迁档目标 | `年度资料专用档口` | `ValidateInteractiveSimulatedTargetAsync`、`ValidateTargetSlotForBatchMoveAsync` |

- **不**允许落在「未设置」或「历史资料专用档口」。
- 历史存档台账启动同步（`SyncHistoryArchivePlacements`）**不**因用途硬拦，避免历史存量数据阻断启动；历史资料应逐步迁入「历史资料专用档口」。

## 测试数据准备

- 需要全量重设时可调用 `ResetAllStandardArchiveSlotsToUnsetCategory`（仅测试场景）。
---
