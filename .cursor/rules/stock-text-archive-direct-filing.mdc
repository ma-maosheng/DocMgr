---
description: 存档文本直办立档（资料室管理员跳过审批，按档案盒登记模拟纸基资料并模拟立档）
globs: "**/{StockTextArchiveDirectFiling*,StockTextArchiveExcel*,StockDirectFilingYearProjectCatalog.cs,ArchiveRegisterDomainValues.cs}"
alwaysApply: false
---

# 存档文本直办立档

资料室资料管理员将过往已按档案盒整理的纸基（模拟）档案纳入年度资料档案化管理：以**档案盒**为单位手工登记后直接完成建档与模拟立档，**无需申请审批**。办结后的立档事实、模拟档案盒、柜位占用与正规「建档申请 → 审批 → 资料立档（模拟）」相同，后期出库/迁档/盘库/处置共用逻辑。

## 办理粒度

- **手工向导仍一盒一提交**：每次确认立档只办理 1 个新档案盒。
- 不提供「并入已有盒」、证明材料扫描、目录扫描。
- **Excel 导入**：一次预览多盒，确认后仍按盒循环调用同一套 `CommitAsync`（一条建档单 + 一个新模拟盒），不平行实现第二套立档。

## 字段与默认值

- 实施年度 / 项目名称：**开放域**下拉，选项来自「项目信息 ∪ 模拟盒 ∪ 电子袋」并集；可选手输新值；选中已有项目时可带回项目编号
- 来源类型：固定 `存量直办`（不可改）
- 提供单位：固定 `资料室`
- 资料名称：默认可改；选项目后默认=项目名称
- 库管模式：默认 `院管资料、长期存档`（可改；写入建档单级字段）
- 密级：仅在资料子项填写；新建子项默认 `秘密`，须逐条确认
- 证明材料：固定 `无`
- 介质：`模拟`；一份直办单一种载体类型，域值为 **打印纸、绘图纸、打印相纸、感光胶片、感光相纸**
- 子项分类（必填，写入 `YearlyArchiveRegisterSimulatedMediaItemDetail`）：资料类型（文本/图件）、所属子类（随类型 Scope）、组织形式（散页/装订）
- 新建子项默认：资料类型 `文本`、组织形式 `装订`、子类取该类型第一项
- 处置：固定 `介质留存`

## 业务编号

直办一次对应 **一条建档单 + 一个模拟档案盒**：

- **档案盒编号**：`年度模拟-{实施年度}-{序号}`（确认写入前预览、不占用；确认时占用）
- **建档单号**：与正规建档共用号段；**年度段**取自项目实施年度
- **立档编号**：`立档-模拟-{年度}-{序号}`；年度取自项目实施年度（Excel 导入为表头「年度」）；`FiledAt` 仍为实际办理时间
- 正规「资料立档」仍按建档申请创建年生成盒号与立档编号，二者不可混用

## 落位

- 手工向导档口：推荐「年度资料专用档口」（标准滑道式；见 `standard-cabinet-slot-category.mdc`）
- 手工向导盒内序号：确认写入时按格口最小可用序号生成 `BoxLocationCode`
- **Excel 导入**：必须使用表内 `档案盒编号`（物理位置，如 `丙A-3-2-01`）解析柜/面/层/列/盒序号，不得改推荐档口、不得重排盒内序号
- 同年度/项目已有模拟盒时：仍**新建盒**，不并入旧盒

## Excel 导入

- 选文件后必须弹出工作表选择（即使只有一张表）；解析指定 sheet，禁止默认取第一张
- 表头至少含：年度、项目名称、资料名称、子项名称、份数、档案盒编号、档案盒规格
- 合并空单元格向下填充；按档案盒编号分组，一盒一条直办单
- 资料名称沿用表中值（如「归档资料」）
- 所属子类按子项名称规则映射，未命中则为「其他」；图类名称资料类型改为「图件」
- 档案盒规格须为系统五档之一；可只在首行填写后下填
- 载体默认打印纸，密级默认秘密，组织形式默认装订
- 目标完整位置未被占用；柜体必须存在
- 标准滑道式档口：允许「年度资料专用档口」「混用档口」；「未设置/未定义」在提交该盒前同步为年度资料专用；已是「历史资料专用档口」或未设置但格内已有历史资料时，自动改为「混用档口」
- 失败盒跳过，不阻断后续盒

## 写入路径

1. 匹配或新建 `ProjectInfo`（匹配键：实施年度 + 项目名称）
2. 合成 `YearlyArchiveRegisterRecord`（`Status=Completed`）及模拟介质图，子项同时写模拟扩展表
3. 调用 `ArchiveFilingService.CreateArchiveBoxAsync` 写盒、链接、柜位与立档事实

不平行实现第二套模拟立档逻辑。
