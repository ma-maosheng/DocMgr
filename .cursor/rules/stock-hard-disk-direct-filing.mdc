---
description: 存量硬盘直办立档（资料室管理员跳过审批，读盘扫描后直接登记并电子立档）
globs: "**/{StockHardDiskDirectFiling*,StockDirectFilingYearProjectCatalog.cs,ArchiveRegisterDomainValues.cs}"
alwaysApply: false
---

# 存量硬盘直办立档

资料室资料管理员将过往年度已在库硬盘纳入年度资料档案化管理：读取硬盘参数、扫描四级目录后直接完成硬盘登记与电子立档，**无需申请审批**。办结后的立档事实、电子袋、硬盘台账与正规「建档申请 → 审批 → 资料立档」相同，后期出库/迁档/盘库共用逻辑。

## 目录与字段

根目录必须为 `年度/项目/资料名称/子项/…`。一块硬盘只允许唯一「年度/项目」。第四层子项视为**目录型根目录**：根下文件与一级子目录一并记入明细（允许混合），不再拆业务字段。存量子项一律记「目录型」，不因「根下只有文件」改成文件型。

实施年度、项目名称为**开放域**下拉：选项来自「项目信息 ∪ 模拟盒 ∪ 电子袋」并集；可选手输尚未登记的新年度/新项目。选中已有项目时可带回项目编号。

## 默认值（可改）

- 来源类型：固定 `存量直办`（不可改）
- 提供单位：固定 `资料室`（系统填入）
- 库管模式：`院管资料、长期存档`
- 密级：`秘密`
- 资料类型：`数据类` / 子类 `最终成果数据`
- 证明材料：`无`（不强制扫描件）
- 项目编号：新建项目时非必填；匹配键为实施年度 + 项目名称

## 业务编号

直办一次对应 **一块硬盘 / 一个电子袋**。确认写入时：
- **电子袋号（介质袋编号）**：`年度电子-{年度}-{序号}`（整盘一条；标题按 YA-DSP-ED 写成 `存量硬盘直办立档 · {预览号}`，确认写入前不占用编号）
- **袋号中的「年度」**：仅本流程取自「目录扫描」卡片中的**项目实施年度**（四级目录第一层 / 页面「年度」字段）。正规「资料立档」仍按建档申请创建年生成袋号，二者不可混用。
- **建档单号**：每份资料名称生成一条入库申请编号（与正规建档共用号段规则；**年度段**同样取自项目实施年度，确认时才占用）
- **立档编号**：`立档-电子-{年度}-{序号}`；编号中的年度取自项目实施年度，`FiledAt` 仍为实际操作时间

确认立档前必须做完整性与逻辑核验（硬盘参数域值、出厂日期、数据档口含档内序号、扫描明细、序列号状态等）。

## 落位与袋子

- 档口：推荐「年度数据硬盘专用档口」空位（含档内序号）。
- 同年度/项目已有硬盘袋时：**新建立第二袋**（新盘、新电子立档单元），不并入旧袋。

## 立档路径命名

同一资料名称、同一目标袋内：首个子项文件夹为 `子项`，其后 `子项_001`、`子项_002`。不回改已占用路径；若袋内已有带序号的同名文件夹，则继续编号。存量导入优先使用盘上真实文件夹名。
