<Window x:Class="DocMgr.Views.Shared.OperationProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, Mode=OneWay}"
        Width="420"
        SizeToContent="Height"
        MinWidth="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="SingleBorderWindow"
        Background="#F5F7FA">
    <Grid Margin="16,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="{Binding StatusText, Mode=OneWay}"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="#334155"
                   Margin="0,0,0,10"/>
        <Grid Grid.Row="1" Height="16">
            <!-- 不确定/确定进度分两个条：WPF 同一 ProgressBar 切换 IsIndeterminate 经常不重绘 -->
            <ProgressBar IsIndeterminate="True">
                <ProgressBar.Style>
                    <Style TargetType="ProgressBar">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsIndeterminate, Mode=OneWay}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ProgressBar.Style>
            </ProgressBar>
            <ProgressBar Minimum="0"
                         Maximum="100"
                         IsIndeterminate="False"
                         Value="{Binding ProgressValue, Mode=OneWay}">
                <ProgressBar.Style>
                    <Style TargetType="ProgressBar">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsIndeterminate, Mode=OneWay}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ProgressBar.Style>
            </ProgressBar>
        </Grid>
        <TextBlock Grid.Row="2"
                   Margin="0,6,0,0"
                   HorizontalAlignment="Right"
                   FontSize="12"
                   Foreground="#64748B"
                   Text="{Binding PercentText, Mode=OneWay}"/>
    </Grid>
</Window>
